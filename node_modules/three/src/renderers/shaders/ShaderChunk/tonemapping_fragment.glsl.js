import React from 'react';
  
  const Tonemapping_fragment.glsl = () =>  {
	return (
	  <div>
	  </div>
	);
  }
  
  export default Tonemapping_fragment.glsl;
  export default /* glsl */`
#if defined( TONE_MAPPING )

	gl_FragColor.rgb = toneMapping( gl_FragColor.rgb );

#endif
`;
