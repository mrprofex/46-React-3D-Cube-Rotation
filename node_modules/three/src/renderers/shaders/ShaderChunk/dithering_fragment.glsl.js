import React from 'react';
  
  const Dithering_fragment.glsl = () =>  {
	return (
	  <div>
	  </div>
	);
  }
  
  export default Dithering_fragment.glsl;
  export default /* glsl */`
#ifdef DITHERING

	gl_FragColor.rgb = dithering( gl_FragColor.rgb );

#endif
`;
